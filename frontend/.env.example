# Em desenvolvimento, deixe vazio: o Vite faz proxy de /api para o backend (porta 4000).
# Ajuste VITE_DEV_API_PROXY no vite.config se o backend usar outra porta.

# Produção: URL absoluta da API (sem barra no final)
# VITE_API_BASE_URL=https://api.exemplo.com
