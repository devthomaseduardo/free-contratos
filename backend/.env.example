# Obrigatório para os endpoints de IA (Google Gemini)
GEMINI_API_KEY=

# Porta HTTP do backend (padrão: 4000)
PORT=4000

# Origem do frontend para CORS (separar múltiplas com vírgula)
FRONTEND_ORIGIN=http://localhost:3000
