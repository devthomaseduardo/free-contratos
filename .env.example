# Copie para backend/.env (recomendado) ou exporte no shell.
# Nunca commite arquivos .env com segredos.

# Obrigatório para IA (Google AI Studio / Vertex conforme sua conta)
GEMINI_API_KEY=

# Backend
PORT=4000
FRONTEND_ORIGIN=http://localhost:3000

# Frontend (produção: URL absoluta da API; em dev deixe vazio para usar o proxy do Vite)
# VITE_API_BASE_URL=https://api.seudominio.com
